<% if current_user.likes.find_by_post_id(post.id) %>

  <%= link_to "Unlike", post_like_path(:id => current_user.likes.find_by_post_id(post.id).id, :post_id => post.id), method: :delete %>

<% else %>

  <%= link_to "Like", post_likes_path(post_id: post.id), method: :post %>

<% end %>
<br>